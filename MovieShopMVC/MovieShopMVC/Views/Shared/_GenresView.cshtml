@using ApplicationCore.ServiceInterfaces

@inject IGenreService _GenreService


@foreach (var genre in await _GenreService.GetAllGenre())
{
    @*@Html.ActionLink(genre.Name, "Genre", "Movies", new { genreId = genre.Id }, new { @class = "dropdown-item" })*@

    <a asp-action="Genre" asp-controller="Movies" class="dropdown-item" asp-route-id="@genre.Id"> @genre.Name</a>
}